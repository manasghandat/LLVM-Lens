# LLVM-Lens

Analyzes LLVM middle-end (`opt`) and backend (`llc`) pass pipelines on a source
file and generates a static HTML report for exploring pass behavior across IR
and Machine IR.

> Scaffolded structure only — implementation pending.
